# Main Quest — 내 업무의 적용 지점 도출

대상 업무: 모두의연구소 캠프 운영 (학습 콘텐츠 제작·운영 팀)
작성일: 2026-08-14

---

## 1. 후보 목록

| 적은 것 | 업무 유형 | 지금 처리 방식 | 병목 | 발생량 | 틀렸을 때 |
| --- | --- | --- | --- | --- | --- |
| **A. 수강생 문의 1차 분류·배분** | 분류와 배분 | 운영 담당자가 문의함을 읽고 결제/일정/과제/기술/기타로 나눠 담당자에게 전달 | 판단 자체는 쉬움. **판단의 총량**이 병목 (건당 30초 × 하루 수십 건) | 하루 40~80건 | 엉뚱한 담당자에게 가서 **응답이 하루 늦어짐**. 되돌릴 수 있음 |
| **B. 과제 제출물 채점 보조** | 검사와 판독 | 튜터가 제출 코드/문서를 읽고 루브릭 항목별 점수 부여 | 일관성과 지속 시간. 튜터가 바뀌면 기준이 미묘하게 달라짐 | 기수당 200~400건, 주 단위 몰림 | 점수가 **수강생에게 그대로 나감**. 성적·수료 여부에 영향. 되돌리기 어려움 |
| **C. 강의 자료 이미지·다이어그램 검색** | 검색 | 담당자가 드라이브 폴더를 열어 눈으로 넘김. 파일명은 `Frame 3.png` 수준 | 자료에 붙은 설명이 없음 | 콘텐츠 개편 시기에 집중, 주 5~10회 | 못 찾으면 **새로 만듦**. 시간 낭비지만 되돌릴 수 있음 |

### 셋 중 하나 고르기

| 기준 | A. 문의 분류 | B. 채점 보조 | C. 자료 검색 |
| --- | --- | --- | --- |
| 판정 기준이 이미 정해져 있다 | ✅ 분류 체계가 문서로 있음 | ⚠️ 루브릭은 있으나 튜터 간 해석이 갈림 | ❌ "찾는 것"의 정의가 질의마다 다름 |
| 지난 판정 기록이 남아 있다 | ✅ 과거 티켓에 배분 이력이 그대로 남음 | ✅ 채점 이력 있음 | ❌ 과거 검색 이력이 남지 않음 |
| 틀렸을 때 되돌릴 수 있다 | ✅ 재배분하면 끝 | ❌ 수강생 성적에 영향 | ✅ |
| 발생량이 충분하다 | ✅ 하루 40~80건 | ⚠️ 기수당 몰림 | ⚠️ 주 5~10회 |

**선정: A. 수강생 문의 1차 분류·배분** — 넷을 모두 만족하는 유일한 지점.

**뺀 이유**
- **B**는 "틀렸을 때 되돌릴 수 없는 일"에 해당합니다. 커리큘럼 2강 기준으로 PoC 대상에서 뒤로 미루고, 넣더라도 **최종 점수가 아니라 후보를 좁혀 튜터에게 넘기는 앞단**에만 넣습니다. 그리고 튜터 셋의 판정이 갈리는 항목이 있다면 필요한 것은 모델이 아니라 **루브릭을 다시 문서로 만드는 작업**입니다.
- **C**는 판정 기록이 없어 채점할 자료를 만들 수 없습니다. PoC보다 **검색 이력을 남기는 절차**가 먼저입니다.

---

## 2. 성공 기준

> **오분류가 100건 중 5건 이하이고, 확신이 낮은 건은 사람에게 넘어간다.**

**숫자의 근거 — 현행 수준을 실제로 쟀는가:**
아직 못 쟀습니다. 이 PoC에서는 **키워드 규칙 기준선의 성능을 현행 수준의 대리값(proxy)** 으로 썼습니다. 이건 정직하게 말하면 **기준의 근거가 아직 비어 있는 상태**입니다.

제대로 하려면: 운영 담당자에게 문의 20건을 배분하게 하고 걸린 시간과 오배분 수를 세야 합니다. 사람이 100건에 2건 틀린다면 5건 틀리는 도구는 도입할 값이 없고, 기준을 3건 이하로 다시 써야 합니다.

**추가 제약 — 20건으로는 5% 기준을 판정할 수 없습니다.** 20건에서 오분류 1건이 곧 5%라, 한 건 차이로 통과와 탈락이 갈립니다. 아래 결과의 오분류율은 **후보를 줄 세우는 데까지만** 쓰고, 도입 판단에는 최소 100건이 필요합니다.

---

## 3. 후보 구성 — 크기가 아니라 종류로

| 후보 | 실행 위치 | 만들어진 목적 | 결과의 형태 |
| --- | --- | --- | --- |
| **기준선.** 키워드 규칙 (지금 쓰는 방식의 코드화) | 노트북 | 이 업무 전용 (수기) | 규칙 적중 비율 |
| **A.** 문자 n-gram TF-IDF + 로지스틱 회귀 | 노트북 | 우리 자료로 학습한 전용 분류기 | 클래스 확률 (softmax — 합이 1) |
| **B.** 다국어 문장 임베딩 + 라벨 설명문 코사인 유사도 | 노트북 | 범용 임베딩 모델 | 라벨마다 독립된 코사인 값 |
| ~~C. 외부 LLM API 분류~~ | ~~외부 서비스~~ | — | — |

### 제외 조건을 점수보다 먼저 걸었습니다

**후보 C(외부 LLM API)는 성능을 재기 전에 제외했습니다.** 문의 본문에는 수강생 실명, 이메일, 카드 결제 내역, 회사명이 그대로 들어 있습니다. 이 자료를 외부로 보낼 수 없습니다.

점수로는 C가 1위였을 가능성이 높습니다. 하지만 점수를 먼저 매기고 감점 항목으로 다뤘다면, 다른 항목이 높아서 **쓸 수 없는 후보가 표의 1위가 되고 자료가 외부로 나간다는 사실은 각주로 밀렸을 것**입니다.

**라이선스:** 남은 후보는 scikit-learn(BSD-3), `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`(Apache 2.0). 사내 업무 사용에 걸리는 조건 없음. (2026-08-14 확인)

---

## 4. 검증 결과

자료 20건, 정답은 **모델을 돌리기 전에** `data/tickets.csv`의 `gold_label`에 적어 두었습니다. 그중 5건은 담당자가 어렵다고 말하는 유형(`difficulty=hard`)입니다 — 두 유형에 걸친 문의(id 13, 14), 중복 문의(id 15), 원인이 애매한 장애(id 19), 문서 오류 신고(id 20).

### 임계값 없이 (thr=0.0)

| 후보 | 정답 | 오분류 |
| --- | --- | --- |
| **기준선. 키워드 규칙** | **18/20** | 2건 |
| B. 임베딩 유사도 | 12/20 | 8건 |
| A. TF-IDF + 로지스틱 | 4/20 | 16건 |

**기준선이 이겼습니다.** 이건 "AI가 쓸모없다"가 아니라 **"이 업무에는 지금 방식이 낫다"** 는 결과이고, 그것도 PoC의 결과입니다. 기준선을 후보에 넣지 않았다면 임베딩 12/20을 보고 "쓸 만하다"고 착각했을 것입니다.

**A가 무너진 이유:** 학습 자료가 19건(leave-one-out)뿐이라 5개 클래스를 가르기에 턱없이 부족합니다. 점수도 전부 0.24~0.36 근방에 몰려 있습니다. **softmax라 합이 1이 되도록 눌린 값**이고, "전부 아니다"가 표현될 자리가 없습니다. 이 후보를 살리려면 모델이 아니라 **라벨링된 과거 티켓 수백 건**이 먼저 필요합니다.

**B가 틀린 자리:** id 17(수료증 발급 → 과제로 오분류), id 20(커리큘럼표 계좌번호 오류 → 결제로 오분류). 라벨 설명문만 보고 표면 어휘에 끌린 결과입니다.

### 임계값 구간 찾기 (기준선)

| 임계값 | 자동 처리 | 오분류 | 오분류율 | 사람에게 넘김 |
| --- | --- | --- | --- | --- |
| 0.0 | 20 | 2 | 10.0% | 0 |
| 0.4 / 0.5 | 17 | 2 | 11.8% | 3 |
| 0.6 / 0.7 | 14 | 1 | 7.1% | 6 |
| **1.0** | **11** | **0** | **0%** | **9** |

임계값을 올리면 오분류가 줄지만 사람에게 넘기는 건수가 같이 늡니다. **성공 기준(≤5%)을 만족하는 임계값은 1.0뿐이고, 그때 자동 처리 비율은 55%입니다.**

### 판정

**조건부 통과.** 오분류 기준은 만족하지만, 병목이 "판단의 총량"이었던 것을 감안하면 **55% 자동화는 도입 근거로 약합니다.** 임계값 0.6~0.7 구간(자동 70%, 오분류 7.1%)이 실무적으로 더 나을 수 있는데, 그건 성공 기준 자체를 다시 써야 판단할 수 있습니다. 임계값을 임의로 0.6으로 정한 뒤 "만족했다"고 적으면 안 됩니다.

---

## 5. 코딩 에이전트 지시문

실제로 `run_poc.py`를 만들 때 쓴 지시문입니다. 6강이 요구한 다섯 항목이 모두 들어 있습니다.

```
수강생 문의를 5개 유형(결제/일정/과제/기술/기타)으로 분류하는 PoC 스크립트를 만들어 주세요.

- 모델은 전부 노트북에서 돌립니다. 문의 본문에 개인정보가 있으므로
  외부 API를 호출하거나 자료를 외부로 전송하지 마세요.
- 후보를 셋 넣어 주세요. 지금 쓰는 키워드 규칙(기준선),
  우리 자료로 학습하는 전용 분류기, 범용 임베딩 유사도.
  기준선을 빼지 마세요. 개선 폭을 재는 기준입니다.
- 판정에 쓰는 값(임계값, 정규화 계수, 모델 이름)을 코드에 박지 말고
  전부 명령행 인자로 받게 해 주세요.
- 건별로 예측 라벨과 점수를 함께 남기고, 점수는 반올림하지 마세요.
  임계값 구간을 찾을 때 씁니다.
- 중간 산출물을 파일로 남기고, 다시 돌려도 기존 파일을 덮어쓰지 마세요.
  임계값을 바꿔 가며 돌린 결과가 전부 남아야 합니다.
- 결과를 눈으로 확인할 수 있는 형태로 함께 저장해 주세요.
  건별 문의 원문·정답·예측·점수가 한 표에 보이는 마크다운이면 됩니다.
```

**마지막 항목이 실제로 검산을 가능하게 했습니다.** TF-IDF 후보의 집계 숫자(4/20)만 봤다면 "자료가 부족하구나" 정도로 넘어갔을 것입니다. 건별 표를 열어 보니 점수가 전부 0.24~0.36에 몰려 있었고, 그제서야 **softmax라 값 자체가 임계값을 걸 수 없는 성질**이라는 것이 보였습니다. 값이 계산된다는 것은 그 값이 의도한 것을 재고 있다는 뜻이 아닙니다.

---

## 6. 오늘의 회고

**1) 고른 것과 뺀 것의 차이는 무엇이었나?**
"틀렸을 때" 칸이 갈랐습니다. 세 후보 모두 발생량과 병목은 그럴듯했는데, B(채점)는 잘못된 판정이 수강생 성적에 그대로 나갑니다. 되돌릴 수 있는지가 나머지 세 기준보다 먼저였습니다.

**2) 성공 기준의 숫자를 어디서 가져왔나? 현행 수준을 실제로 재 보았나?**
**재지 않았습니다.** 커리큘럼의 예시 형태를 그대로 가져와 5%로 썼고, 현행 수준의 대리값으로 키워드 기준선을 썼습니다. 이건 이 PoC의 가장 약한 고리입니다. 담당자 20건 실측이 다음 순서입니다.

**3) 실행 위치나 라이선스 때문에 제외한 후보가 있었나? 점수로는 몇 위였나?**
외부 LLM API 후보를 실행 위치 조건으로 제외했습니다. **점수를 재지 않았으므로 몇 위인지 모릅니다 — 재지 않은 것이 맞습니다.** 조건에 걸리는 후보는 성능이 얼마든 제외이고, 점수를 매겨 두면 "이게 제일 잘하는데" 하는 미련이 남아 결국 표에 다시 올라옵니다.

**4) 기준을 넘지 못한 항목이 있다면 다음에 무엇을 바꿔 다시 확인하겠는가?**
자동 처리 비율이 55%에 그친 것이 남은 문제입니다. 순서대로 셋을 바꿔 보겠습니다.
1. **자료를 20건에서 100건으로 늘립니다.** 지금 오분류율은 한 건 차이로 5%p씩 움직여 판단 근거가 못 됩니다.
2. **현행 수준을 실측해 성공 기준의 숫자를 다시 씁니다.** 사람이 10% 틀리는 업무라면 7.1%는 이미 개선입니다.
3. **그다음에 후보 A를 다시 봅니다.** 과거 티켓 수백 건에 라벨을 붙이면 전용 분류기가 살아날 수 있고, 그때는 softmax가 아니라 **라벨마다 독립된 확률(one-vs-rest)** 로 내게 해야 임계값을 걸 수 있습니다.

모델을 더 좋은 것으로 바꾸는 것은 이 셋 다음입니다.

---

## 재현 방법

```bash
python run_poc.py --candidates baseline_keyword,tfidf_logreg,embedding_siglip_like --threshold 0.0
python run_poc.py --candidates baseline_keyword --threshold 1.0
```

산출물은 `results/` 에 남습니다. 같은 이름이 있으면 `__2`, `__3` 을 붙여 덮어쓰지 않습니다.
